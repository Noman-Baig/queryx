name: queryx_example
description: Runnable examples of queryx usage, including a Dio transport adapter.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  queryx:
    path: ../
  dio: ^5.7.0

dev_dependencies:
  lints: ^4.0.0
